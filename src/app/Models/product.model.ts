export class Product {
    productId!: number;
    productName!: string;
    price!: number;
    categoryId!: number;
    description!: string;
    imagePath?: string; // Optional field for image path
    imagePath1?: string; 
    imagePath2?: string; 
   // category?: Category; // Optional, if needed to store the category details
  }
  